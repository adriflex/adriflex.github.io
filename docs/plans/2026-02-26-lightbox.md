# Lightbox Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Afficher les images des articles en grand dans un overlay au clic, sans quitter la page.

**Architecture:** Composant Astro `Lightbox.astro` contenant le HTML, le CSS (scoped) et le JS (inline script). Importé uniquement dans les deux pages articles. Déclenchement par délégation d'événement sur `.prose img`.

**Tech Stack:** Astro 5, Tailwind v4, vanilla JS TypeScript (pas de librairie externe).

---

### Task 1 : Créer `src/components/Lightbox.astro`

**Files:**
- Create: `src/components/Lightbox.astro`

**Step 1 : Créer le fichier avec ce contenu exact**

```astro
---
// Aucune prop nécessaire
---

<div id="lightbox" aria-hidden="true" aria-modal="true" role="dialog">
  <div id="lightbox-backdrop"></div>
  <button id="lightbox-close" aria-label="Fermer">×</button>
  <img id="lightbox-img" src="" alt="" />
</div>

<style>
  #lightbox {
    position: fixed;
    inset: 0;
    z-index: 10000; /* au-dessus du grain (z-index: 9999 dans global.css) */
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity 200ms ease;
  }

  #lightbox.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  #lightbox-backdrop {
    position: absolute;
    inset: 0;
    background-color: rgba(26, 22, 20, 0.92);
    cursor: zoom-out;
  }

  #lightbox-close {
    position: absolute;
    top: 1.25rem;
    right: 1.25rem;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    border: 1px solid rgba(245, 239, 227, 0.2);
    background: transparent;
    color: rgba(245, 239, 227, 0.7);
    font-size: 1.5rem;
    line-height: 1;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: color 200ms ease, border-color 200ms ease;
    z-index: 1;
  }

  #lightbox-close:hover {
    color: #E8622A;
    border-color: #E8622A;
  }

  #lightbox-img {
    position: relative;
    z-index: 1;
    max-width: 90vw;
    max-height: 90vh;
    object-fit: contain;
    border-radius: 0.5rem;
    opacity: 0;
    transform: scale(0.95);
    transition: opacity 250ms ease, transform 250ms ease;
  }

  #lightbox.is-open #lightbox-img {
    opacity: 1;
    transform: scale(1);
  }
</style>

<script>
  const lightbox = document.getElementById('lightbox') as HTMLElement;
  const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
  const lightboxClose = document.getElementById('lightbox-close') as HTMLElement;
  const lightboxBackdrop = document.getElementById('lightbox-backdrop') as HTMLElement;

  function openLightbox(src: string, alt: string) {
    lightboxImg.src = src;
    lightboxImg.alt = alt;
    lightbox.classList.add('is-open');
    lightbox.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.classList.remove('is-open');
    lightbox.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';
  }

  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    if (target.matches('.prose img')) {
      openLightbox((target as HTMLImageElement).src, target.getAttribute('alt') ?? '');
    }
  });

  lightboxClose.addEventListener('click', closeLightbox);
  lightboxBackdrop.addEventListener('click', closeLightbox);

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && lightbox.classList.contains('is-open')) {
      closeLightbox();
    }
  });
</script>
```

**Step 2 : Vérifier que le fichier existe**

```bash
ls src/components/Lightbox.astro
```
Expected : le fichier est listé.

---

### Task 2 : Ajouter `cursor: zoom-in` aux images prose

**Files:**
- Modify: `src/styles/global.css`

**Step 1 : Ajouter à la fin du fichier**

```css
/* Curseur zoom sur les images dans le contenu Markdown */
.prose img {
  cursor: zoom-in;
}
```

---

### Task 3 : Importer Lightbox dans `projects/[slug].astro`

**Files:**
- Modify: `src/pages/projects/[slug].astro`

**Step 1 : Ajouter l'import dans le frontmatter**

Ligne 3 (après `import { getCollection } from 'astro:content';`), ajouter :

```astro
import Lightbox from '../../components/Lightbox.astro';
```

**Step 2 : Ajouter le composant dans le template**

Juste avant la balise fermante `</Base>` (ligne 114), ajouter :

```astro
  <Lightbox />
```

---

### Task 4 : Importer Lightbox dans `lab/[slug].astro`

**Files:**
- Modify: `src/pages/lab/[slug].astro`

**Step 1 : Ajouter l'import dans le frontmatter**

Ligne 3 (après `import { getCollection } from 'astro:content';`), ajouter :

```astro
import Lightbox from '../../components/Lightbox.astro';
```

**Step 2 : Ajouter le composant dans le template**

Juste avant la balise fermante `</Base>` (ligne 58), ajouter :

```astro
  <Lightbox />
```

---

### Task 5 : Build + vérification manuelle

**Step 1 : Lancer le build**

```bash
npm run build
```
Expected : `dist/` généré sans erreur TypeScript ni Astro.

**Step 2 : Lancer le preview**

```bash
npm run preview
```

**Step 3 : Checklist de test manuel**

Ouvrir un article avec des images (ex. `/lab/2026-02-26-ai-nodes-blender`).

- [ ] Le curseur devient `zoom-in` au survol d'une image
- [ ] Cliquer sur une image → l'overlay apparaît avec fade + scale
- [ ] L'image s'affiche correctement (centrée, max 90vw/90vh)
- [ ] Clic sur le fond sombre → ferme
- [ ] Clic sur le bouton × → ferme (hover passe en orange)
- [ ] Touche `Escape` → ferme
- [ ] Le scroll de la page est bloqué pendant l'ouverture
- [ ] Le scroll est restauré après fermeture
- [ ] Fonctionne sur mobile (tester responsive)

**Step 4 : Commit**

```bash
git add src/components/Lightbox.astro src/styles/global.css src/pages/projects/[slug].astro src/pages/lab/[slug].astro
git commit -m "feat: add lightbox for article images"
```
